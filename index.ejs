<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>
    <%- htmlWebpackPlugin.options.metadata.title %>
  </title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <base href="<%- htmlWebpackPlugin.options.metadata.baseUrl %>">
  <!-- Need this for the icons in froala-editor -->
  <!-- imported CSS are concatenated and added automatically -->
</head>

<body>
  <nav id="topNav">
    <!-- top navigation placeholder; any customization applied on nav here won't work, as the outerHTML is replaced by TopNavBar Webpart -->
  </nav>
  <div id="splash">
    <!-- splash screen container -->
  </div>
  <div id="aurelia-app" aurelia-app="main"></div>
  <% if (htmlWebpackPlugin.options.metadata.server) { %>
    <!-- Webpack Dev Server reload -->
    <script src="/webpack-dev-server.js"></script>
    <% } %>
</body>

</html>